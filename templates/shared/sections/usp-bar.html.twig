<section class="border-y border-aperture-line bg-white">
    <div class="ap-container grid divide-y divide-aperture-line md:grid-cols-2 md:divide-x md:divide-y-0 lg:grid-cols-4">
        {% for usp in usps|default([]) %}
            <div class="my-2 md:my-4 md:px-6 first:pl-0 last:pr-0">
                {% include 'shared/components/usp-card.html.twig' with { usp: usp } only %}
            </div>
        {% endfor %}
    </div>
</section>
